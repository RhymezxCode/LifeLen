# LifeLens API keys — TEMPLATE.
#
# 1. Copy this file to `secrets.properties` in the repo root.
# 2. Fill in your keys (see docs/API-KEYS.md for how to obtain them).
# 3. `secrets.properties` is gitignored — NEVER commit real keys.
#
# You can also leave these blank and enter the keys at runtime in the app's Settings screen.

# Qwen vision + text models, via Alibaba Cloud DashScope (Model Studio).
DASHSCOPE_API_KEY=

# Web/shopping search for live pricing (Serper.dev by default; see docs/API-KEYS.md).
SEARCH_API_KEY=
